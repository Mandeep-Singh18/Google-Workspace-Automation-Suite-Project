import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#4285F4' }, // Google Blue
    secondary: { main: '#0F9D58' }, // Google Green
  },
});
export default theme;